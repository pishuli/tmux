My tmux config backup

Installation:
$ cd
$ rm .tmux.conf
$ git clone https://github.com/jarsonfang/tmux.git
$ ln -s tmux/tmux.conf .tmux.conf

add the following line to ~/.bash_aliases file:
alias tmux='tmux -2 new-session -s develop'
